# 基于事件驱动的短视频平台 —— 性能测试方案

> 复现并扩展毕业论文《基于事件驱动的短视频平台设计与实现》第 6 章（测试环境与方法 / 功能测试 / 性能测试 / 测试结果分析）的实验，给出一套可在本机或 4C8G 虚机上端到端落地的测试方案。

---

## 1. 测试目标

对照论文 §6 的三个核心结论，把它们都"可复现"：

| 编号 | 结论（论文原文核心数据） | 本方案需要复现/验证的指标 |
|---|---|---|
| G1 | Kitex 最大 ~98,000 QPS、TP99 ~7.8 ms；gRPC ~52,000 QPS、TP99 ~18 ms | RPC 层最大吞吐、TP99 |
| G2 | 同步直连数据库在 10,000 QPS 洪峰下约 30 s 崩溃（OOM）；Kafka 异步架构 TP99 < 80 ms 不崩溃 | 20 倍洪峰下系统稳定性 |
| G3 | 核心接口 TP99 < 100 ms（非功能需求 §2.3） | 核心 HTTP 接口端到端延迟 |
| G4 | 千并发同时点赞，Redis 计数与 MySQL 落库一致 | 异步落库一致性 |

---

## 2. 测试环境

按论文 §6.1 严格对齐，避免"打不出真实瓶颈"。

### 2.1 硬件

- **CPU**：4 核（在 macOS 本机用 `colima start --cpu 4 --memory 8` 限定容器 CPU/内存上限）
- **内存**：8 GB
- **磁盘**：SSD ≥ 50 GB
- **网络**：被测端、压测端在同一 LAN 段；如本机自压，尽量绑核避免争抢

### 2.2 软件版本（与项目锁定一致）

- Go 1.24、Node 25、Python 3.9
- Docker / colima、MySQL（分库分表）、Redis、Kafka、ES、etcd、MinIO、Milvus
- Hertz（HTTP）、Kitex（RPC）

### 2.3 压测/观测工具

| 用途 | 工具 | 备注 |
|---|---|---|
| RPC 层吞吐 | `go-stress-testing` 或 `ghz` | 论文使用 go-stress-testing；ghz 对 gRPC 更标准 |
| HTTP 端到端 | JMeter 或 `wrk` / `k6` | 论文用 JMeter；k6 脚本化更适合 CI |
| 链路追踪 | Jaeger | 论文已接入；按服务名过滤 |
| 系统指标 | Prometheus + Grafana / `node_exporter` + `mysqld_exporter` + `kafka_exporter` | 看 CPU/内存/网络/连接池 |
| 日志 | ELK / Loki | 看错误率与 OOM Kill |

### 2.4 启动顺序

```bash
# 1. 基础组件（一次性）
cd /Users/zhihuaxu/Desktop/go/Refactored-TikTok
docker compose -f docker-compose.middleware.yml up -d   # mysql/redis/kafka/etcd/minio/es

# 2. 依次启动微服务（每个服务独立终端，便于看日志）
go run ./cmd/user
go run ./cmd/video
go run ./cmd/interaction
go run ./cmd/relation
go run ./cmd/recommend
go run ./cmd/notification
go run ./cmd/api          # 网关，最后启动
```

确认 `/healthz` 全部 200 后再开压。

---

## 3. 关键度量定义

为了让结论可比对、可复现，统一规定指标：

- **QPS**：单位时间内成功响应的请求数（HTTP 2xx / RPC OK），不含失败重试。
- **TP50 / TP90 / TP99 / TP999**：分位延迟。论文重点关注 TP99。
- **错误率**：5xx + RPC 非 OK + 超时 / 总请求。> 0.1 % 视为不达标。
- **稳定时长**：连续不发生错误 / OOM 的最长持续秒数（用于洪峰测试）。
- **数据一致性**：Redis 计数 与 MySQL 终态记录数的差值 = 0。

测试时间窗：每轮 ≥ 2 分钟，前 30 s 不计入（warm-up）。

---

## 4. 测试矩阵（共 6 个用例）

> 命名遵循 `Pxx-模块-场景` ，与论文 §6.2/§6.3/§6.4 一一对应。

### P01 · RPC 通信框架对比（复现论文表 6-1）

**目的**：复现 Kitex vs gRPC 的吞吐 / TP99 差距（约 1.9 倍）。

**被测**：`interaction` 服务的 `DoLike` 方法，分别用 Kitex 与 gRPC stub 直连，绕过 HTTP 网关。

**载荷**：1 KB 包，固定 100 并发。

**步骤**：
1. 用 Kitex 客户端跑 5 分钟，记录 max QPS / TP99 / TP999 / 最大延迟。
2. 用 `protoc` 生成 gRPC stub（保持业务逻辑不变），重复一次。
3. 对比指标。

**示例命令**（go-stress-testing 风格）：

```bash
# Kitex
go run scripts/perf/kitex_dolike_bench.go -c 100 -n 5000000 -duration 300s

# gRPC（备用 stub）
ghz --insecure --proto idl/interaction.proto \
    --call interaction.InteractionService.DoLike \
    -d '{"video_id":1001,"user_id":1}' \
    -c 100 -n 5000000 127.0.0.1:8889
```

**通过标准**：Kitex QPS ≥ 1.5×gRPC、Kitex TP99 ≤ gRPC TP99 × 0.6，方向与论文一致即可。

---

### P02 · 核心 HTTP 接口基准延迟（验证 TP99 < 100 ms）

**目的**：验证非功能需求 §2.3 — 核心接口 TP99 < 100 ms。

**被测接口**（来源于 `cmd/api/router/`）：

| 接口 | 方法 | 说明 |
|---|---|---|
| `/v1/user/login` | POST | 登录拿双 Token |
| `/v1/feed` | GET | 推荐 Feed 流 |
| `/v1/action/like` | POST | 点赞 |
| `/v1/comment/list` | GET | 评论列表 |
| `/v1/notification/unread` | GET | 拉未读通知 |

**载荷曲线**：阶梯递增 50 → 100 → 200 → 500 → 1000 并发，每阶 2 分钟。

**k6 脚本骨架**：

```javascript
// scripts/perf/p02_core_api.js
import http from 'k6/http';
import { check, sleep } from 'k6';

export const options = {
  stages: [
    { duration: '2m', target: 50 },
    { duration: '2m', target: 100 },
    { duration: '2m', target: 200 },
    { duration: '2m', target: 500 },
    { duration: '2m', target: 1000 },
  ],
  thresholds: {
    http_req_duration: ['p(99)<100'],   // TP99 < 100ms
    http_req_failed:   ['rate<0.001'],
  },
};

const BASE = 'http://127.0.0.1:8080';
const TOKEN = __ENV.ACCESS_TOKEN;

export default function () {
  const headers = { Authorization: `Bearer ${TOKEN}` };
  check(http.get(`${BASE}/v1/feed?last_time=0`, { headers }),    { 'feed 200':    r => r.status === 200 });
  check(http.post(`${BASE}/v1/action/like`,
        JSON.stringify({ video_id: 1001 }),
        { headers: { ...headers, 'Content-Type':'application/json' } }), { 'like 200':  r => r.status === 200 });
  sleep(0.1);
}
```

**通过标准**：每个接口 TP99 < 100 ms、错误率 < 0.1 %。

---

### P03 · 20 倍洪峰下的容灾对比（复现论文表 6-2）

**目的**：复现"传统同步 vs Kafka 异步"在 20× 洪峰下的差距，论文中同步方案 ~30 s 崩溃。

**被测**：`/v1/action/like` 接口，对照两种实现：

- **A 组（对照，传统同步）**：临时关闭 Kafka 生产者，让 InteractionService 直接 `INSERT video_likes`，跳过 Redis 缓存写入路径。
  > 在 `cmd/interaction/service/like.go` 增加 `LIKE_SYNC=1` 环境变量分支，仅压测时使用。
- **B 组（本系统）**：保持 Redis-先行 + Kafka 异步落库。

**流量曲线**：

```
0 ─────────15s─────────16s──────────────120s
500 QPS ───────────────► 突增到 10,000 QPS ───►
```

**JMeter 压测计划**：

```
Thread Group: warm
  ConstantThroughputTimer: 500 req/s
  Duration: 15s
Thread Group: peak
  ConstantThroughputTimer: 10000 req/s
  Duration: 105s
```

**观测项**：
- 峰值期 TP99
- 错误率（含 5xx、超时、连接重置）
- 是否发生进程被 OOM Kill（看 `dmesg | grep -i kill`）
- 洪峰结束后队列消化时间（Kafka lag 归零所用秒数）

**通过标准（与论文一致即可）**：
- A 组：TP99 在 30 s 内突破 5000 ms，可观察到崩溃 / OOM
- B 组：洪峰期 TP99 < 80 ms，过程无崩溃，洪峰结束后 5 分钟内 Kafka lag 归零

---

### P04 · 千并发点赞数据一致性（复现论文 §6.2 第 4 个场景）

**目的**：1000 并发同时给同一条视频点赞，验证 Redis 即时计数 与 MySQL 终态写入条数一致。

**步骤**：
1. 准备 1000 个用户 token，写入 `tokens.csv`。
2. 用 k6 在 60 s 内每用户发送 1 次 like 请求；总请求量约 1000。
3. 等待 5 分钟，让 Kafka 消费完毕。
4. 校验：

```sql
-- 期望数值 = 1000
SELECT COUNT(*) FROM video_likes WHERE video_id = 1001;
```

```bash
# 期望数值 = 1000
redis-cli GET video:like_count:1001
redis-cli SCARD video:likes:1001
```

**通过标准**：三者全部等于 1000，差值为 0。

---

### P05 · 视频上传断点续传（复现论文 §6.2 第 2 个场景）

**目的**：200 MB 文件分片上传过程中断网，恢复后只续传失败分片。

**步骤**：
1. 准备 `bigvideo.mp4`（200 MB）。
2. 调 `/v2/publish/uploading` 获取 uploadID。
3. 客户端逐片上传（5 MB / 片）。第 20 片时 `tc qdisc add dev lo root netem loss 100%` 触发断网。
4. 恢复网络后客户端重试，只补传失败的片。
5. 上传完成后比对 MinIO 中文件 MD5 与本地文件 MD5。

**通过标准**：MD5 一致；MinIO 上传日志显示同一分片仅成功 1 次。

---

### P06 · Feed 复合游标翻页不重复（复现论文 §6.2 第 3 个场景）

**目的**：连续翻页几十页，不出现重复视频。

**步骤**：
1. 调 `/v1/feed?last_time=0&last_id=0`，记录返回的 `(created_at, video_id)` 列表与下一页游标。
2. 用上一次返回的尾部游标作为新请求的 `last_time` / `last_id`，循环 30 次。
3. 累计所有 video_id 入集合，校验 `len(set) == sum(len(page))`。

**通过标准**：30 页内零重复。

---

## 5. 目录结构与脚本组织

建议在仓库内新建：

```
Refactored-TikTok/
└── scripts/
    └── perf/
        ├── README.md
        ├── colima-bootstrap.sh        # 启动 4C8G 容器环境
        ├── seed_data.sql              # 造 10w 用户 / 100w 视频
        ├── gen_tokens.go              # 批量生成压测 token
        ├── p01_kitex_vs_grpc.sh       # P01 入口
        ├── p02_core_api.js            # P02 k6 脚本
        ├── p03_burst.jmx              # P03 JMeter 计划
        ├── p03_toggle_sync.sh         # P03 切换 LIKE_SYNC=1/0
        ├── p04_consistency.js
        ├── p05_chunk_upload.py
        └── p06_feed_cursor.py
```

---

## 6. 数据准备

避免"空库测出虚高 QPS"：

- 用户：100,000，密码 bcrypt
- 视频：1,000,000（MinIO 中可只放 1 个真实文件，其余记录复用同一 object）
- 关注关系：平均粉丝/关注 50
- 点赞历史：5,000,000 条，覆盖热点视频
- Redis 预热：把 Top 1000 热门视频的点赞集合 / 计数预热进去
- ES：索引视频标题，触发 search 路径

```bash
# 一键造数据
mysql -h127.0.0.1 -uroot -p tiktok < scripts/perf/seed_data.sql
go run scripts/perf/gen_tokens.go -n 1000 -out tokens.csv
```

---

## 7. 观测与定位

每轮压测必看的看板：

- **API 网关**：Hertz QPS、TP99、错误率（按路由分组）
- **RPC**：Kitex 客户端/服务端 RT、连接数、协程数
- **MySQL**：连接池占用、慢查询、`Threads_running`、`Innodb_row_lock_waits`
- **Redis**：QPS、命中率、阻塞命令、慢日志
- **Kafka**：生产 RPS、消费 RPS、Lag、ISR
- **JVM/Go runtime**：GC pause、heap、goroutine 数
- **机器**：CPU%、load、网卡 pps、丢包

异常时按 Jaeger 看一条慢请求的 span 树，按耗时占比定位（论文中也是这种方法）。

---

## 8. 报告模板

每跑完一轮输出 `report-YYYYMMDD-HHMM.md`，至少含：

```markdown
## 用例：P03 · 20 倍洪峰对比
- 时间：2026-05-18 20:30
- 环境：colima 4C/8G，MySQL 8.0.36，Kafka 3.7
- 流量：500 QPS × 15s → 10000 QPS × 105s
- 结果：

| 组 | 峰值 QPS | 峰值 TP99 | 错误率 | 是否崩溃 | 洪峰结束后 lag 归零耗时 |
|---|---|---|---|---|---|
| A · 同步 | 813 | 5,420 ms | 32 % | 第 28 s OOM Kill | — |
| B · Kafka 异步 | 9,980 | 76 ms | 0.02 % | 否 | 4 min 12 s |

- 结论：与论文表 6-2 趋势一致 ✅
- 复盘：A 组在第 22 s 看到 MySQL `Threads_running` 飙到 1024，连接池耗尽，随后 GC 频繁直到 OOM
- 截图：见 `figs/p03-grafana.png`、`figs/p03-jaeger.png`
```

---

## 9. 上线检查清单

正式答辩复现前过一遍：

- [ ] colima 资源限制 4C8G 已生效（`colima status`）
- [ ] 6 个微服务 + 网关 `/healthz` 全 200
- [ ] MySQL `max_connections=1000`，`innodb_buffer_pool_size=2G`
- [ ] Redis `maxmemory=1gb`，`maxmemory-policy=allkeys-lru`
- [ ] Kafka 3 副本，`num.partitions=8`
- [ ] Jaeger Agent 接入，所有服务能上报
- [ ] Prometheus 抓到全部 `/metrics`
- [ ] 数据集已 seed，token 文件已生成
- [ ] 关闭非必要后台进程，避免争抢 CPU
- [ ] 已对 P03 准备 LIKE_SYNC 环境变量切换路径

---

## 10. 与论文的对应关系

| 论文位置 | 本方案用例 |
|---|---|
| §6.1 测试环境与方法 | 第 2、3 节 |
| §6.2 功能测试（5 个场景） | P02 / P04 / P05 / P06（双 Token 续签场景以单元测试形式覆盖即可） |
| §6.3.1 通信框架性能对比（表 6-1） | P01 |
| §6.3.2 架构容灾能力对比（表 6-2） | P03 |
| §6.4 测试结果分析 | 第 8 节 报告模板 |

---

完成上述 6 个用例后，你的答辩 PPT 第 6 章就能直接贴自己的数据，而不是引用论文里的数字 —— 这通常是评委最爱追问的点。
